--- a/app/routers/market_view.py
+++ b/app/routers/market_view.py
@@ -112,7 +112,7 @@
     msg = await cb.bot.send_message(
         cb.message.chat.id,
         f"<b>Барахолка → {city.name}</b>",
         reply_markup=markup,
         parse_mode="HTML"
     )
-    last_bot_messages[chat_id] = [msg.message_id]
+    last_bot_messages.setdefault(chat_id, []).extend([msg.message_id])
     await cb.answer()
 
--- a/app/routers/market_view.py
+++ b/app/routers/market_view.py
@@ -470,9 +470,8 @@
         reply_markup=await market_inline()
     )
- last_bot_messages.setdefault(chat_id, []).extend([query_msg.message_id])
- last_bot_messages.setdefault(chat_id, []).extend([nav_msg.message_id])
-    last_bot_messages[chat_id] = [msg.message_id]
+    last_bot_messages.setdefault(chat_id, []).extend([query_msg.message_id])
+    last_bot_messages.setdefault(chat_id, []).extend([nav_msg.message_id])
     await cb.answer()
 
     print(
